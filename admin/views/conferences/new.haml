%ul.nav.nav-tabs
  -#%li=link_to tag_icon(:list, pat(:list)), url(:conferences, :index)
  %li.active=link_to tag_icon(:plus, pat(:new)), url(:conferences, :new)

.tabs-content
  -form_for :conference, url(:conferences, :create), :class => 'form-horizontal' do |f|
    =partial 'conferences/form', :locals => { :f => f }
